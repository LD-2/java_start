base标签是html中的标签 一般写到head标签中 针对于html 语言的一些路径 只要是不以/开头的路径 都自动加上这个base路径  对js代码中的路径不起作用
注意利用JSTL标签的forEach时 数据是存到域中的用el表达式取数据
注意删除cookie 时间0和路径绑定


过滤器是servlet中的规范 拦截器是springmvc中的规范
改动后OA项目的问题:
    一个业务要写一个是否登入的判断 代码冗余
